# 超值限时组合包购买规划规则

本文档记录当前规划器采用的规则假设，适用于 `PackCalculatorView.vue` 中的购买方案规划功能。

路径计算算法见 `path-planning-algorithm.md`。

## 档位

超值限时组合包的档位由付费钻数量决定。项目数据用日元价格表示，同一档位的对应关系如下：

| 价格 | 付费钻 |
| ---: | ---: |
| 160 | 80 |
| 650 | 325 |
| 1000 | 500 |
| 1500 | 750 |
| 3000 | 1500 |
| 6000 | 3000 |
| 11800 | 5900 |

初始触发的限时包通常是 80 付费钻档，也就是价格 160 档。玩家当前已经处在更高档时，可以在规划器中把“当前档位”改成对应价格。

## 升降档

- 触发并购买至少 1 个限时包后，下个结算批次升 1 档。
- 触发但没有购买，2 小时后超时，下个结算批次降 1 档。
- 档位不会低于最低档，也不会高于最高档。

## 卡包批次

当 2 小时内连续触发多个限时包时，这些礼包视作同一批：

- 在最后一个礼包消失前，批内至少购买 1 个，则下一批升 1 档。
- 批内其他礼包不买，不会额外导致降档。
- 批内全部不买，则下一批降 1 档。
- 规划器用“每批最多触发数”模拟 2 小时内可连续触发多少个礼包。

## 触发节奏

- 主线和无穷塔可以随推关连续触发，适合把“每批最多触发数”设为大于 1。
- 单个属性塔每天只能爬 10 层，不能像无穷塔一样持续触发，适合设为 1 或当天实际可触发数。
- 玩家等级提升较慢，通常适合设为 1。
- 不同类别的限时包可以同时触发；主线、玩家等级、无穷塔、各属性塔在规划器中视作独立触发源。
- 规划器会先按每个触发源独立生成批次，再把各触发源的第 N 批合并为同一个全局结算批次，统一应用升降档规则。

页面字段含义：

- 预算（日元）：本次可用于购买限时组合包和可选常驻补包的金额上限。
- 当前礼包档位：现在已经处于的限时组合包档位。
- 当前层/等级：该触发源目前已经达到的位置；规划器只会考虑大于该值的触发点。
- 计划推到：本次规划中该触发源最多推进到的位置。
- 主线的当前进度和计划推到支持 `章节-关卡` 格式，例如 `13-28`；也支持直接填写游戏内连续关卡进度，例如 `336`。其他来源使用数字层数或等级。
- 2小时内最多触发：该触发源在一个卡包批次内最多能连续触发多少个限时包。
- 允许常驻钻石组合包补累充（非首次）：规划器可在每个已买限时包的批次后，把非首次常驻钻石组合包作为本批补累充候选。

## 全属性塔抵达

“全属性塔抵达”不是独立填写的触发源，而是由蓝塔、红塔、翠塔、黄塔自动派生：

```
全属性塔进度 = min(蓝塔层数, 红塔层数, 翠塔层数, 黄塔层数)
```

只有四个属性塔都在规划器中启用，并且四塔共同跨过某个“全属性塔抵达”礼包层级时，该礼包才会进入对应批次。

派生批次按四塔中最慢达到该层的塔计算：

```
触发批次 = max(蓝塔达到该层的批次, 红塔达到该层的批次, 翠塔达到该层的批次, 黄塔达到该层的批次)
```

因此无需为“全属性塔抵达”单独填写起始层、目标层或每批触发数。

## 最优路径目标

规划器在预算内最大化购买礼包的总评分价值。礼包内容价值继续使用 `items-calc-formula.md` 中定义的 CE 评分逻辑；购买规划页中的每日累充赠钻按实际累计付费钻计算，不使用单包 `1.2` 倍估算。

若预算用不完，是因为继续购买会超出预算，或后续在当前档位没有可购买礼包。

规划器不会在输入变化时自动重算；需要用户点击“计算购买方案”。这样可以避免多触发源全部启用时，输入框每次变化都触发动态规划。

为了控制状态规模，动态规划每个批次后会按档位做状态裁剪：保留预算-价值维度上的 Pareto 状态，并限制每个档位最多保留一批高价值候选路径。

## 每日累充与常驻补包

每日累充赠钻规则记录在 `../items-calc-formula.md`。购买规划页按路径内同一天累计付费钻计算实际赠钻：

```text
本次累充赠钻 = TotalBonusFreeDiamonds(购买后累计付费钻) - TotalBonusFreeDiamonds(购买前累计付费钻)
```

当前规划器按单个累充日计算，默认本次规划不会跨过 0 点。若实际购买跨过 0 点，0 点前后的付费钻应拆成两个累充日，当前结果会与真实赠钻不同。

开启“允许常驻钻石组合包补累充（非首次）”后，规划器会在每个已经购买限时组合包的批次后，额外枚举“本批不补”和“本批补到下一累充档”两类候选：

- 只使用 `permanentPacks.json` 中非首次的“钻石组合包”。
- 补包附着在对应的买入批次上，不会单独生成独立展示行。
- 补包不会触发限时包，不会影响限时包升降档，但会消耗预算并改变后续动态规划选择。
- 补包只瞄准购买前的下一档每日累充；达到该档后立即停止，即使预算还有剩余也不会多买常驻包。
- 补包采用能达到下一累充档的最低花费组合；若花费相同，优先内容价值更高、包数更少的组合。
- 页面按批次展示最终方案实际采用的补包，例如 `第 1 批：钻石组合包 80 ×2`，不展示未被采用的补包候选。

例如限时包累计到 5,900 付费钻时，可以用两个 80 付费钻常驻包补到 6,060 付费钻，从而解锁 6,000 档每日累充奖励。

## 多方案展示

页面会同时生成多种方案，供玩家按偏好切换：

- 价值最优：用动态规划在预算内寻找总评分价值最高的路径。
- 次优方案：同一动态规划中保留候选路径，展示价值排名第二的路径。
- 只买小包：只在最低档购买，升档后等待超时掉回最低档。
- 冲最大包：先每批买 1 个用于升档，到最高档后尽量购买高档礼包。

这些方案共用同一套触发批次和升降档规则，只是购买动作选择策略不同。

## 属性塔原始开放规则

属性塔本身存在真实日期限制：

| 星期 | 开放属性塔 |
| --- | --- |
| 周一 | 蓝塔 |
| 周二 | 红塔 |
| 周三 | 翠塔 |
| 周四 | 黄塔 |
| 周五 | 蓝塔、红塔 |
| 周六 | 翠塔、黄塔 |
| 周日 | 蓝塔、红塔、翠塔、黄塔 |

属性塔按每天 4 点换日。每个单属性塔每天最多推进 10 层。若玩家在 4 点换日前后 2 小时内卡包，单个属性塔在同一个限时包批次内理论上最多可以跨两天推进：

```text
跨日同批最大推进 = 10 + 10 = 20 层
```

每日累充按 0 点重置，属性塔按 4 点换日。这是两套不同时间规则：

```text
累充重置：00:00
属性塔换日：04:00
```

当前乐观卡包策略不使用跨 4 点额外推塔能力，因此不需要处理“跨 0 点累充重置”和“跨 4 点属性塔换日同批”的互斥。若未来新增精确跨 4 点属性塔模式，则同一个 2 小时批次不能同时作为跨 0 点累充批次和跨 4 点属性塔批次。

## 乐观卡包与属性塔拓扑

规划器默认采用“乐观卡包”假设：玩家会主动控制主线、无穷塔、属性塔等来源的触发时机，让可安排的礼包尽量落入同一个 2 小时卡包批次。

该假设不代表可以任意打乱属性塔事件顺序。属性塔仍按触发层级建立拓扑序列：

```text
全属性塔抵达 225
单属性塔 250（蓝/红/翠/黄可同批）
全属性塔抵达 275
单属性塔 300（蓝/红/翠/黄可同批）
...
```

全属性塔抵达由四塔最低层决定：

```text
全属性塔进度 = min(蓝塔层数, 红塔层数, 翠塔层数, 黄塔层数)
```

当该进度跨过全属性塔触发层时，才生成全属性塔抵达礼包。

属性塔相关礼包的相邻触发事件至少相隔 25 层：

- 全属性塔抵达与下一个单属性塔触发相隔 25 层。
- 单属性塔触发与下一个全属性塔抵达相隔 25 层。
- 同类事件相隔 50 层。

即使考虑 4 点换日前后 2 小时卡包，单塔最多也只能推进 20 层，仍无法跨过相邻属性塔礼包事件。因此当前规划器忽略跨 4 点合批能力，并把蓝塔、红塔、翠塔、黄塔的“2小时内最多触发”固定为 1。

## 日期抽象原因

当前规划器将真实日期抽象掉，不要求用户填写开始日期或规划天数，原因如下：

1. 规划器采用乐观卡包假设，认为玩家会主动卡塔，把属性塔尽量停在触发点前，并在可同步窗口集中触发。
2. 当前属性塔相关礼包事件的相邻间隔至少是 25 层：
   - 全属性塔抵达 225 -> 单属性塔 250，相隔 25 层。
   - 单属性塔 250 -> 全属性塔抵达 275，相隔 25 层。
   - 同类事件之间相隔 50 层。
3. 跨 4 点同批最多推进 20 层，小于相邻事件的 25 层间隔，因此跨 4 点不会让两个相邻属性塔礼包事件合并进同一个批次。
4. 真实星期只影响玩家准备卡点的过程，不改变礼包事件的拓扑顺序。

因此算法只保留“属性塔事件拓扑序”，不模拟真实日历：

```text
全属性塔抵达 225
单属性塔 250（蓝/红/翠/黄可同批）
全属性塔抵达 275
单属性塔 300（蓝/红/翠/黄可同批）
...
```

该抽象依赖当前礼包触发数据。如果未来属性塔或全属性塔礼包触发间隔缩短到 20 层以内，需要重新评估是否必须引入真实日期/开放窗口模型。
