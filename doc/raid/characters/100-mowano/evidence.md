# 穆瓦诺：讨伐拉表接入证据

- MB：`CharacterMB[100]`、`ActiveSkillMB[100001/100002]`、`PassiveSkillMB[100003/100004]`；档位 Lv240 / EX3。
- 日志：`y1800mrl.json` 中穆瓦诺为 `Guid 11`。首回合护盾 `10000430101` 是 `SkillCategory: 4`，且文本明确“无法被解除”。
- S1 的物防降低 `10000120102` 与脱力 `10000130105` 分别为独立 Boss 弱化组；前者物防 -25% 并进入物防路，后者仍为零倍率状态。
- `100003301` 的 `PassiveTrigger: 32` 与鹭鸶提卡行动开始被动相同；日志中 `Guid 11` 的复制结果写入 `ActionStartSubSkillResults`，首回合复制朝日的可解除 Group 并保留 `GranterGuid: 41`。Lv240 复制 Buff 数最高其他友军的全部可复制 Buff，首回合后每经过 4 次自身行动再次触发。
- 复制得到的可解除 Buff 参与穆瓦诺自身 Buff 数和攻击倍率；攻击率复制为“被复制友军 ATK / 穆瓦诺 ATK”的独立符号项，原施加者仅保留为 Buff 来源。不可解除状态不复制。敌方冷却延迟不在当前木桩边界内。
