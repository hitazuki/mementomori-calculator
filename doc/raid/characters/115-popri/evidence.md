# 波普莉：讨伐拉表接入证据

## 接入条件

- CharacterMB：`115`；流金，速度 `3036`，物理普通攻击。
- 采用档位：Lv240、专武 EX3。S1 的 6 段来自 EX1；EX3 只将命中提高至 5%，该数值在木桩模型中不结算但已记录。
- 证据：`CharacterMB[115]`、`ActiveSkillMB[115001/115002]`、`PassiveSkillMB[115003/115004]`；尚无受控战斗日志。

## 技能与被动清单

| 来源 | 游戏数据（Lv240 / EX3） | 讨伐模型处理 | 确认程度 |
| --- | --- | --- | --- |
| S1 `115001` 灵言之风 | 先获得“言灵魔咒”1 层、最多 5 层；有至少 2 名其他流金友方则改为 +2 层。3 层前 210%，3 层起 630%，EX1 后 6 段；每层另加命中 5%（EX3）。 | 计数器在伤害前更新，结算 6 段 210%/630%；命中不入倍率。 | 游戏数据；计数先后由描述确认。 |
| S2 `115002` 急冻之声 | 4 段 340%；伤害前 3 层以上为 1020%，恰好 5 层为 2720%。 | 按当前“言灵魔咒”计数结算 4 段。 | 游戏数据。 |
| 被动 `115003` | 每 4 次触发：自身 4 行动暴击抗性 +60%、防御 +100%、物防 +100%。 | 三个可驱散 EffectGroup 保留，用于 Buff 数；防御侧数值不进倍率。 | 游戏数据；全局回合调度为项目映射，待日志确认。 |
| 被动 `115004` | 每 4 次触发：全体 4 行动“增益效果护罩”；开战自身承伤 -60%。 | 护罩保留为每名友方的可驱散 Buff，可触发相关 Buff 数机制；承伤不进倍率。 | 游戏数据；Buff 类别已按项目记录。 |

## EffectGroup / 状态记录

| EffectGroupId | 内容 | 目标与时钟 | 当前处理 |
| --- | --- | --- | --- |
| `11500150101` | 言灵魔咒 | 自身，永久，最多 5 层 | 运行时计数器；命中 +5% 不结算。 |
| `11500340101` | 暴击抗性 | 自身，4 次自身行动 | 可驱散状态，计入 Buff 数；数值不算。 |
| `11500330102` | 防御 | 自身，4 次自身行动 | 可驱散状态，计入 Buff 数；数值不算。 |
| `11500330103` | 物理防御 | 自身，4 次自身行动 | 可驱散状态，计入 Buff 数；数值不算。 |
| `11500400201` | 增益效果护罩 | 全体，4 次目标自身行动 | 可驱散状态，计入 Buff 数。 |
| `11500430101` | 承伤降低 | 自身，开战后持续 | 不可驱散状态；数值不算。 |

## 未计入倍率但已记录

- EX3 的命中 +5%/层、暴击抗性、防御、物防、承伤降低：当前模型不结算命中或防御侧数值。
- 三个“老师的教诲”EffectGroup 与“增益效果护罩”即使没有倍率，也保留为可驱散 Buff，避免未来 Buff 数联动丢失。

## 待日志确认

- 两个“每 4 次触发”被动在全局回合开始的具体先后，以及护罩是否在同一时点覆盖全部友方。
