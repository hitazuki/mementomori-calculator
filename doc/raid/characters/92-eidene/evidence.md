# 艾蒂涅：讨伐拉表接入证据

- CharacterMB `92`，翠属性，速度 `3261`，魔法普通攻击；Lv240 / EX3。
- `45-57.json` 第2回合 S2 的三次 `ChangeHp` 各自触发一次旺花，计数连续增长三层，证明三次回复不能合并成一次事件。
- 旺花目标为自身及攻击力最高的两名其他友军。非翠目标使用 `9200320101`，翠目标使用 `9200320102`；两者均为 `SkillCategory: 4`，攻击均为每层5%，仅反击率存在1%/2%差异。
- S2 第7回合前单木桩480%，第7回合起取消三次回复并提高至1440%；S1 的最大HP和再生不触发即时主动回复事件。

分类：C（复用目标事件并增加元素排除条件）。
