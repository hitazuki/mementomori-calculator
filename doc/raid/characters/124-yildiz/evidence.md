# 耶尔德兹：讨伐拉表接入证据

- CharacterMB `124`，翠属性，速度 `3146`，物理普通攻击；Lv240 / EX3。
- `g1820-2.json` 中主动回复后，`12400430101` 在自身和攻击最高其他友军上于主体伤害前增长；`SkillCategory: 4`，每层攻击和命中各+8%，最多10层。
- S2 在回复触发第10层后立即从4段280%切换为4段1120%，读取点位于本次回复之后、伤害之前。
- S1 单木桩680%，伤害后 `12400120202` 以 `SkillCategory: 3` 施加6回合增益无效；木桩只保留该零倍率弱化组。

分类：C（回复事件）；伤害阈值复用计数器档位。
