# 蜜拉：讨伐拉表接入证据

- CharacterMB `80`，翠属性，速度 `3158`，魔法普通攻击；采用 Lv240 / EX3。
- S1 `80001`：随机3体各180%，日志确认伤害与延迟判定后才回复自身；单木桩结算一次。`8000120102` 为 `SkillCategory: 3`、持续1回合。
- S2 `80002`：先回复自身，再进行4段480%；回复后内部组 `8000500101` 的计数达到6时，本次即使用4段1920%。
- `45-57.json` 中蜜拉自己的S1回复使 `8000500101` 从0变1；其他角色只回复自己时不会增加该计数，确认事件必须保留回复目标。
- 被动 `80003/80004` 只涉及最大HP与再生；木桩模型忽略数值，但没有把再生误记为主动技能即时回复。

分类：C（通用、携带回复目标的 `activeSkillHeal` 事件）。
