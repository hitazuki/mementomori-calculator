# 米赫里：讨伐拉表接入证据

## 接入条件

- CharacterMB：`126`；流金，速度 `3136`，魔法普通攻击。
- 采用档位：Lv240、专武 EX3。S1 专武仅强化护盾数值；S2 的 280% / 1120% 来自 EX3。
- 证据：`CharacterMB[126]`、`ActiveSkillMB[126001/126002]`、`PassiveSkillMB[126003/126004]`；尚无受控战斗日志。

## 技能与被动清单

| 来源 | 游戏数据（Lv240 / EX3） | 讨伐模型处理 | 确认程度 |
| --- | --- | --- | --- |
| S1 `126001` 薪火相传 | 前两次：全体 4 行动护盾后，随机 4 敌各 410% 魔法伤害；第 3 次起不施加护盾，改为 1640%。EX1 强化护盾数值。 | 单木桩结算一次 410%/1640%；护盾文本未写不可解除，作为可解除状态保留并计入 Buff 数，数值不算。 | 游戏数据。 |
| S2 `126002` 炎火幻景 | 4 段；EX3 后前两次 280%，第 3 次起 1120%。最后一段后自身获得 1 行动“轻快”，行动结束时额外恢复 1 点冷却。 | 4 段，`afterDamage` 施加可解除轻快并提供冷却恢复 +1；计入 Buff 数。 | 游戏数据；冷却消耗时序由项目规则。 |
| 被动 `126003` | 自身行动开始时“阳焰灯火” +1、最多 15 层；任意友方有 4 个 Buff 时改为 +2；同时给自身和攻击最高的其他友方，每层攻击 +5%。 | 行动开始计数，并向自身和固定攻击优先级最高的其他友方施加动态攻击倍率。 | 游戏数据；“攻击最高”并列规则为项目约定。 |
| 被动 `126004` | 自身承伤 -75%（EX2）、物理暴伤缓和 +30%。 | 不进入倍率；两项均记录为模型边界。 | 游戏数据。 |

## EffectGroup / 状态记录

| EffectGroupId | 内容 | 目标与时钟 | 当前处理 |
| --- | --- | --- |
| `12600300101` | 阳焰灯火 | 自身及攻击最高的其他友方，永久，最多 15 层 | 每层 `attackRate +5%`，计数器驱动。 |
| `12600140101` | S1 护盾 | 全体，4 次目标自身行动 | 可解除 Buff；护盾数值不算，但计入 Buff 数。 |
| `12600230301` | 轻快 | 自身，1 次自身行动 | 可解除 Buff，`cooldownRecoveryBonus +1`；作用于本次行动的基础冷却恢复并计入 Buff 数。 |
| `126004401` | 承伤降低 | 自身，开战后持续 | 数值不算，仅记录。 |
| `126004301` | 物理暴伤缓和 | 自身，开战后持续 | 数值不算，仅记录。 |

## 未计入倍率但已记录

- S1 全部护盾数值、流金目标的额外护盾值、承伤 -75%、物理暴伤缓和 +30%。
- 这些词条不会改变当前木桩倍率；护盾状态仍保留，便于未来接入护盾相关判定。

## 待日志确认

- “轻快”的实际消耗和冷却恢复顺序；当前采用“行动结束时基础 -1 与额外 -1 同时结算”。
- “攻击最高的其他友方”同值时的游戏原生并列规则。
