# ［暗黑圣耀星］史黛拉：讨伐拉表接入证据

- CharacterMB `132`，翠属性，速度 `2968`，魔法普通攻击；Lv240 / EX3。日文官方名为 `［聖なる漆黒の輝く星］ステラ`。
- `g1820-2.json` 中 S1 先回复并触发 `13200430101`，再施加承伤与魔防弱化，最后造成伤害；因此本次伤害同时吃到新增攻击层和承伤增加。
- 回合1/5使用 `13200130201/30202`（承伤+10%/魔防-20%）；第7回合日志使用 `13200130203/30204`（承伤+20%/魔防-40%）。模型用 `replacementKey` 将晚档替换早档，承伤进入增减伤渠道，魔防降低单独进入魔防路。
- S2 第7回合前一次回复事件包含自身与攻击最高其他友军两个目标，但全局回复触发只增加一层；第7回合起不回复，伤害后按概率施加 `13200230501` 沉默。
- `13200430101` 为 `SkillCategory: 4`，每层攻击+10%、物防+6%，最多10层；只计算攻击。

分类：C（回复目标事件与Boss状态替换槽）；回合7冷却重置复用现有机制。
