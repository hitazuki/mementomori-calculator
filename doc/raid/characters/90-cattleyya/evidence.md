# 嘉德利亚：讨伐拉表接入证据

## 接入条件

- CharacterMB：`90`；流金，速度 `2984`，物理普通攻击。
- 采用档位：Lv240、专武 EX3。主动技能按其最终可用档位取值；无专武强化的技能保留 Lv240 技能等级值。
- 证据：`CharacterMB[90]`、`ActiveSkillMB[90001/90002]`、`PassiveSkillMB[90003/90004]`。尚无该角色受控战斗日志。
- 当前模型：单体木桩、无防御/护盾/敌方行动；防御侧效果保留记录，不改变伤害倍率。

## 技能与被动清单

| 来源 | 游戏数据（Lv240 / EX3） | 讨伐模型处理 | 确认程度 |
| --- | --- | --- | --- |
| S1 `90001` 春化作用 | 先获得 4 行动攻击 +20%；第 2 次起 +40%，EX1 后第 3 次起 +80%；随后随机敌人 4 × 590% 物理伤害。 | `beforeDamage` 读取本次前 S1 历史，施加持续 4 次自身行动的攻击提升；单体木桩结算 4 段。 | 游戏数据；历史读取时点为项目约定。 |
| S2 `90002` 三重分生组织 | 物防最低敌人：第 1/2/3 次为 6/9/12 段；EX3 后每段 470%。 | 单体木桩结算对应段数及 EX3 倍率；“物防最低”在单木桩下无选择差异。 | 游戏数据。 |
| 被动 `90003` | 第 8 回合结束时，解除自身及攻击最高的其他友方 2 个弱化；若另有至少 2 名流金友方，第 4、12 回合也触发。 | 不模拟弱化移除；已用 `raidIgnoredDebuffCleanse` 明示。触发目标、次数和回合保留在本文，待未来有弱化状态模型时接入。 | 游戏数据；全局回合映射待日志确认。 |
| 被动 `90004` | 回合开始获得 4 行动护盾（EX2 值为最大 HP 的 200%）；开战承伤 -60%；回合开始防御 +150%。 | 护盾与防御作为可解除 Buff 保留并计数；承伤降低为不可解除状态。三者数值不进入伤害倍率。 | 游戏文本默认规则；当前模型边界。 |

## EffectGroup / 状态记录

| 标识 | 内容 | 目标与时钟 | 当前处理 |
| --- | --- | --- | --- |
| `9000140103` | S1 攻击提升 | 自身，4 次自身行动 | 文本未写“无法被解除”，按可解除 Buff 计数。 |
| `9000440101` | 护盾 | 自身，4 次自身行动，每 4 个全局回合 | 文本未写“无法被解除”，按可解除 Buff 计数。 |
| `9000420101` | 承伤降低 | 自身，开战后持续 | 文本明确“无法被解除”；暂按不可解除状态，不计 Buff 数。 |
| 被动 `90003` 的解除效果 | 解除弱化 2 个 | 自身及攻击最高的其他友方；第 4/8/12 全局回合条件触发 | 明确忽略，i18n：`raidIgnoredDebuffCleanse`。 |
| `9000430101` | 防御提升 | 自身，4 次自身行动，每 4 个全局回合 | 文本未写“无法被解除”，作为独立可解除 Buff 计数。 |

## 未计入倍率但已记录

| 词条 | 原因 | 未来接入所需数据 |
| --- | --- | --- |
| 弱化效果解除 | 当前没有己方弱化的施加、选择、移除链路。 | 受控日志确认第 4/8/12 回合、目标并列和移除优先级。 |
| 护盾数值、防御、承伤降低 | 当前拉表在防御结算前停止。 | 角色 HP/防御面板、护盾吸收顺序、敌方攻击与承伤公式。 |

## 待日志确认

- `90003` 的“回合结束”是全局回合结束；目前仅作为未来扩展记录，未以近似规则参与结算。
- “攻击最高的其他友方”并列时的实际选取顺序，以及弱化效果的移除优先级。
